# 라즈베리파이 trigger_node 의존성
# 설치 방법:
#   sudo apt install -y portaudio19-dev   # sounddevice 사전 요구사항
#   pip install -r requirements_rpi.txt

RPi.GPIO==0.7.1
sounddevice==0.5.1
faster-whisper==1.1.1
numpy>=1.24.0

# Whisper API 방식 사용 시 (로컬 모델 대신 GMS 사용)
# requests==2.34.2
